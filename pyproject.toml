[project]
name = "algoritmi-distribuiti-notes"
version = "0.1.0"
description = "Notes site with MkDocs, Material, and PDF export"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "mkdocs>=1.6.0",
  "mkdocs-material>=9.5.0",
  "mkdocs-exporter>=6.2.0",
  "pymdown-extensions>=10.0",
]

[tool.uv]
package = false
